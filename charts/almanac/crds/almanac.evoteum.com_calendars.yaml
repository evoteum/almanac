---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: calendars.almanac.evoteum.com
spec:
  group: almanac.evoteum.com
  names:
    kind: Calendar
    listKind: CalendarList
    plural: calendars
    singular: calendar
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Calendar is the Schema for the calendars API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              instances:
                description: Instances are absolute time windows. Default and required
                  for most events.
                items:
                  description: |-
                    CalendarInstance is a one-shot or year-specific absolute time window.
                    Use this for events whose dates change each year (e.g. Easter, Cheltenham
                    Festival) or events that occur only once.
                  properties:
                    end:
                      format: date-time
                      type: string
                    start:
                      format: date-time
                      type: string
                  required:
                  - end
                  - start
                  type: object
                type: array
              recurring:
                description: |-
                  Recurring are cron-expression windows for fixed-date annually repeating events.
                  These are passed directly to KEDA cron triggers.
                items:
                  description: |-
                    RecurringWindow is a time window defined by cron expressions, repeating on
                    the schedule implied by the expressions. Use this for fixed-date annual
                    events (e.g. Christmas, New Year) where the date never changes.
                  properties:
                    end:
                      description: End is a cron expression for when the window closes,
                        e.g. "0 0 27 12 *".
                      minLength: 9
                      type: string
                    start:
                      description: Start is a cron expression for when the window
                        opens, e.g. "0 0 25 12 *".
                      minLength: 9
                      type: string
                    timezone:
                      default: UTC
                      description: Timezone is the IANA timezone for the cron expressions,
                        e.g. "Europe/London".
                      type: string
                  required:
                  - end
                  - start
                  type: object
                type: array
            type: object
          status:
            description: CalendarStatus defines the observed state of Calendar.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
